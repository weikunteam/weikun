<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.great.dao.ProductionDao">
    <select id="getImg" resultType="String">
		select
		img_address
		from tb_production
		where
		production_no = #{productionNo} and user_id = #{userId}
	</select>

    <insert id="saveImg">
        insert into tb_production (production_no,user_id,img_address) values (#{productionNo},#{userId},#{path})
    </insert>

    <update id = "updateImg">
        update tb_production set img_address = #{path} where production_no = #{productionNo} and user_id = #{userId}
    </update>
</mapper>