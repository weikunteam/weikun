<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.great.dao.UserCenterDao">
    <update id="updateUser">
        update TB_USER
        set
        userName = #{userName},
        <if test="age!=null and age!=''">
            age = #{age},
        </if>
        <if test="age == null or age ==''">
            age = null,
        </if>
        job = #{job}
        where
        userId = #{userId}
    </update>

    <select id="listService" resultType="map">
        select name,
               paxydBusId as id,
               applicantTel,
               applyDate,
               type
        from TB_BUS_PAXYD
        where salesManId = #{userId}
  <if test="searchText !=null and searchText != ''">
    and applicantTel = #{searchText}
  </if>
    </select>

    <update id="updateSuggestion">
        update TB_BUS_PAXYD set salesManRemarkAndAdvice = #{suggestion}
where paxydBusId = #{paxydBusId}
    </update>

    <select id="myService" resultType="map">
        select name,
               paxydBusId as id,
               applicantTel,
               applyDate,
               type
        from TB_BUS_PAXYD
        where userId = #{userId}
    </select>
</mapper>